% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{col_heatmap}
\alias{col_heatmap}
\title{Column helper: Heatmap (color intensity)}
\usage{
col_heatmap(
  field,
  header = NULL,
  width = NULL,
  palette = c("#f7fbff", "#08306b"),
  min_value = NULL,
  max_value = NULL,
  decimals = 2,
  show_value = TRUE,
  ...
)
}
\arguments{
\item{field}{Field name containing numeric values}

\item{header}{Column header (default NULL, uses field name)}

\item{width}{Column width in pixels (NULL for auto-sizing based on content)}

\item{palette}{Character vector of 2+ hex colors for interpolation
(default: light blue to dark blue)}

\item{min_value}{Minimum value for color scale (NULL = auto from data)}

\item{max_value}{Maximum value for color scale (NULL = auto from data)}

\item{decimals}{Number of decimal places (default 2)}

\item{show_value}{Show the numeric value over the color (default TRUE)}

\item{...}{Additional arguments passed to \code{web_col()}, including cell styling:
\code{bold}, \code{italic}, \code{color}, \code{bg}, \code{emphasis}, \code{muted}, \code{accent} (column names)}
}
\value{
A ColumnSpec object
}
\description{
Display a number with interpolated background color based on value.
Useful for showing magnitude via color intensity.
}
\examples{
# Default blue gradient
col_heatmap("correlation")

# Custom red-yellow-green palette
col_heatmap("score", palette = c("#d73027", "#fee08b", "#1a9850"))

# Fixed range
col_heatmap("pct", min_value = 0, max_value = 100)

# Hide numeric value (color only)
col_heatmap("value", show_value = FALSE)
}
