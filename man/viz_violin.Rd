% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{viz_violin}
\alias{viz_violin}
\title{Visualization column: Violin plot}
\usage{
viz_violin(
  ...,
  header = "",
  width = 150,
  scale = c("linear", "log"),
  axis_range = NULL,
  axis_ticks = NULL,
  axis_gridlines = FALSE,
  bandwidth = NULL,
  show_median = TRUE,
  show_quartiles = FALSE,
  axis_label = "Value",
  show_axis = TRUE
)
}
\arguments{
\item{...}{One or more \code{effect_violin()} objects defining the violins}

\item{header}{Column header (default "")}

\item{width}{Column width in pixels (default 150)}

\item{scale}{Scale type: "linear" (default) or "log"}

\item{axis_range}{Numeric vector of length 2 specifying axis range c(min, max).
If NULL (default), range is computed automatically from data.}

\item{axis_ticks}{Numeric vector of explicit tick positions. If NULL (default),
ticks are computed automatically.}

\item{axis_gridlines}{Show gridlines at tick positions (default FALSE)}

\item{bandwidth}{KDE bandwidth. NULL (default) uses Silverman's rule of thumb.}

\item{show_median}{Show median indicator line (default TRUE)}

\item{show_quartiles}{Show Q1/Q3 indicator lines (default FALSE)}

\item{axis_label}{Label for the x-axis (default "Value")}

\item{show_axis}{Show the x-axis (default TRUE)}
}
\value{
A ColumnSpec object with type = "viz_violin"
}
\description{
Renders violin plots (kernel density estimation). Requires raw array data
for each row.
}
\examples{
# Single violin per row
viz_violin(effect_violin(data = "values"))

# Multiple violins per row
viz_violin(
  effect_violin(data = "treatment", label = "Treatment", color = "#3b82f6"),
  effect_violin(data = "control", label = "Control", color = "#22c55e"),
  show_median = TRUE,
  show_quartiles = TRUE
)
}
