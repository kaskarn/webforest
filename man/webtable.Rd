% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webtable.R
\name{webtable}
\alias{webtable}
\title{Create an interactive web table}
\usage{
webtable(
  x,
  ...,
  zoom = 1,
  auto_fit = TRUE,
  max_width = NULL,
  max_height = NULL,
  show_zoom_controls = TRUE,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{x}{Either a WebSpec object or a data.frame/data.table/tibble}

\item{...}{Arguments passed to \code{web_spec()} when x is a data frame.
Common arguments: \code{point}, \code{lower}, \code{upper}, \code{label}, \code{group},
\code{columns}, \code{theme}, \code{interaction}}

\item{zoom}{Initial zoom level (0.5 to 2.0, default 1.0)}

\item{auto_fit}{When TRUE (default), shrink content to fit container if too large}

\item{max_width}{Maximum container width in pixels (NULL for none)}

\item{max_height}{Maximum container height in pixels (NULL for none)}

\item{show_zoom_controls}{Show zoom controls on hover (default TRUE)}

\item{width}{Widget width (default NULL for auto)}

\item{height}{Widget height (default NULL for auto)}

\item{elementId}{HTML element ID (optional)}
}
\value{
An htmlwidget object
}
\description{
\code{webtable()} renders a web-native, interactive table visualization
without the forest plot graphical column. Useful for displaying
tabular data with sorting, filtering, and other interactive features.
}
\examples{
\dontrun{
data <- data.frame(
  item = c("A", "B", "C"),
  value = c(1.2, 0.8, 1.5),
  lo = c(0.9, 0.5, 1.1),
  hi = c(1.6, 1.2, 2.0),
  n = c(100, 150, 75)
)

# Create spec
spec <- web_spec(
  data,
  point = "value",
  lower = "lo",
  upper = "hi",
  label = "item",
  columns = list(col_n(), col_interval())
)

# Table only (no forest column)
webtable(spec)

# With forest column
forest_plot(spec)
}

}
\seealso{
\code{\link[=web_spec]{web_spec()}} for creating specifications, \code{\link[=forest_plot]{forest_plot()}} for forest plot rendering
}
