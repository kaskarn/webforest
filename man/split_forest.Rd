% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_forest.R
\name{split_forest}
\alias{split_forest}
\title{Split a forest plot by variable values}
\usage{
split_forest(x, by, shared_axis = FALSE, ...)
}
\arguments{
\item{x}{A WebSpec object or data frame. If a data frame, additional arguments
must be provided to specify point/interval columns.}

\item{by}{Column name(s) for splitting. Can be:
\itemize{
\item Single string: Creates one plot per unique value, e.g., \code{"sex"}
\item Character vector: Creates plots for each combination, with hierarchical
navigation, e.
\item \code{c("region", "age_group")} creates Region > Age Group hierarchy
}}

\item{shared_axis}{Whether to use the same axis range across all plots.
Default is \code{FALSE} (each plot auto-scales to its data).}

\item{...}{Additional arguments passed to \code{web_spec()} if \code{x} is a data frame.}
}
\value{
A SplitForest object containing multiple WebSpec objects
}
\description{
Creates a collection of separate forest plots, one for each unique value
(or combination of values) in the splitting column(s). This is useful for
comparing results across subgroups, regions, or other categorical variables.
}
\examples{
\dontrun{
# Split by a single variable
data |>
  web_spec(point = "or", lower = "lower", upper = "upper") |>
  split_forest(by = "region")

# Split by multiple variables (hierarchical navigation)
data |>
  web_spec(point = "or", lower = "lower", upper = "upper") |>
  split_forest(by = c("sex", "age_group"))

# With shared axis for easier comparison
data |>
  web_spec(point = "or", lower = "lower", upper = "upper") |>
  split_forest(by = "treatment_arm", shared_axis = TRUE)
}

}
