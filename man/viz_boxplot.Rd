% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{viz_boxplot}
\alias{viz_boxplot}
\title{Visualization column: Box plot}
\usage{
viz_boxplot(
  ...,
  header = "",
  width = 150,
  scale = c("linear", "log"),
  axis_range = NULL,
  axis_ticks = NULL,
  axis_gridlines = FALSE,
  show_outliers = TRUE,
  whisker_type = c("iqr", "minmax"),
  axis_label = "Value",
  show_axis = TRUE
)
}
\arguments{
\item{...}{One or more \code{effect_boxplot()} objects defining the boxplots}

\item{header}{Column header (default "")}

\item{width}{Column width in pixels (default 150)}

\item{scale}{Scale type: "linear" (default) or "log"}

\item{axis_range}{Numeric vector of length 2 specifying axis range c(min, max).
If NULL (default), range is computed automatically from data.}

\item{axis_ticks}{Numeric vector of explicit tick positions. If NULL (default),
ticks are computed automatically.}

\item{axis_gridlines}{Show gridlines at tick positions (default FALSE)}

\item{show_outliers}{Show outlier points beyond whiskers (default TRUE)}

\item{whisker_type}{Whisker calculation: "iqr" (1.5*IQR, default) or "minmax"}

\item{axis_label}{Label for the x-axis (default "Value")}

\item{show_axis}{Show the x-axis (default TRUE)}
}
\value{
A ColumnSpec object with type = "viz_boxplot"
}
\description{
Renders box-and-whisker plots. Supports either raw array data (quartiles
computed automatically) or pre-computed summary statistics.
}
\examples{
# Boxplot from array data (quartiles computed automatically)
viz_boxplot(effect_boxplot(data = "values"))

# Boxplot from pre-computed statistics
viz_boxplot(effect_boxplot(
  min = "min_val", q1 = "q1_val", median = "median_val",
  q3 = "q3_val", max = "max_val"
))

# Multiple boxplots per row
viz_boxplot(
  effect_boxplot(data = "group_a", label = "Group A", color = "#3b82f6"),
  effect_boxplot(data = "group_b", label = "Group B", color = "#22c55e")
)
}
