% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{viz_bar}
\alias{viz_bar}
\title{Visualization column: Bar chart}
\usage{
viz_bar(
  ...,
  header = "",
  width = 150,
  scale = c("linear", "log"),
  axis_range = NULL,
  axis_ticks = NULL,
  axis_gridlines = FALSE,
  axis_label = "Value",
  show_axis = TRUE
)
}
\arguments{
\item{...}{One or more \code{effect_bar()} objects defining the bars to display}

\item{header}{Column header (default "")}

\item{width}{Column width in pixels (default 150)}

\item{scale}{Scale type: "linear" (default) or "log"}

\item{axis_range}{Numeric vector of length 2 specifying axis range c(min, max).
If NULL (default), range is computed automatically from data.}

\item{axis_ticks}{Numeric vector of explicit tick positions. If NULL (default),
ticks are computed automatically.}

\item{axis_gridlines}{Show gridlines at tick positions (default FALSE)}

\item{axis_label}{Label for the x-axis (default "Value")}

\item{show_axis}{Show the x-axis (default TRUE)}
}
\value{
A ColumnSpec object with type = "viz_bar"
}
\description{
Renders horizontal bar charts with support for multiple effects (grouped bars).
Each row displays one or more bars based on data values.
}
\examples{
# Single bar per row
viz_bar(effect_bar("value"))

# Multiple bars per row (grouped)
viz_bar(
  effect_bar("baseline", label = "Baseline", color = "#3b82f6"),
  effect_bar("followup", label = "Follow-up", color = "#22c55e")
)
}
