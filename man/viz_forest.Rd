% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-components.R
\name{viz_forest}
\alias{viz_forest}
\title{Visualization column: Forest plot}
\usage{
viz_forest(
  header = NULL,
  width = NULL,
  point = NULL,
  lower = NULL,
  upper = NULL,
  effects = NULL,
  scale = c("linear", "log"),
  null_value = NULL,
  axis_label = "Effect",
  axis_range = NULL,
  axis_ticks = NULL,
  axis_gridlines = FALSE,
  show_axis = TRUE,
  annotations = NULL,
  shared_axis = NULL,
  ...
)
}
\arguments{
\item{header}{Column header (default NULL, typically no header for plot)}

\item{width}{Column width in pixels (NULL for auto-sizing based on available space)}

\item{point}{Column name for point estimate. Use for single-effect plots.}

\item{lower}{Column name for lower bound. Use for single-effect plots.}

\item{upper}{Column name for upper bound. Use for single-effect plots.}

\item{effects}{List of \code{effect_forest()} objects for multi-effect display
(multiple markers overlaid in same column). Cannot be used with point/lower/upper.}

\item{scale}{Scale type: "linear" (default) or "log"}

\item{null_value}{Reference line value. Default is 0 for linear scale, 1 for log scale.}

\item{axis_label}{Label for the x-axis (default "Effect")}

\item{axis_range}{Numeric vector of length 2 specifying fixed axis limits c(min, max).
If NULL (default), axis range is computed automatically from data.}

\item{axis_ticks}{Numeric vector specifying tick mark positions. If NULL (default),
ticks are computed automatically.}

\item{axis_gridlines}{Logical; if TRUE, display vertical gridlines at tick positions
(default FALSE).}

\item{show_axis}{Show the x-axis (default TRUE)}

\item{annotations}{List of annotation objects (e.g., \code{forest_refline()}) for this column.}

\item{shared_axis}{When used in a split forest, whether this column should use a shared
axis range across all splits. \code{NULL} (default) inherits from split-level setting,
\code{TRUE}/\code{FALSE} overrides.}

\item{...}{Additional arguments passed to \code{web_col()} (e.g., \code{sortable})}
}
\value{
A ColumnSpec object with type = "forest"
}
\description{
Renders a forest plot (point estimates with confidence intervals) as a
table column. This allows explicit positioning of the forest plot within
the column layout and supports multiple forest columns per table.
}
\details{
Each \code{viz_forest()} fully owns its effect definitions - no global effects
list is needed. Use either inline column references (point/lower/upper) for
a single effect, or a list of \code{effect_forest()} objects for multiple effects.
}
\examples{
# Single forest column with inline effect definition
viz_forest(point = "estimate", lower = "ci_lower", upper = "ci_upper")

# Log scale with custom null line
viz_forest(point = "hr", lower = "hr_lo", upper = "hr_hi",
           scale = "log", null_value = 1, axis_label = "Hazard Ratio")

# Multiple effects overlaid in one column
viz_forest(
  effects = list(
    effect_forest("itt_or", "itt_lo", "itt_hi", label = "ITT", color = "#2563eb"),
    effect_forest("pp_or", "pp_lo", "pp_hi", label = "Per-Protocol", color = "#16a34a")
  ),
  scale = "log",
  null_value = 1,
  axis_label = "Odds Ratio (95\% CI)"
)
}
