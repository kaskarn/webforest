% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{set_zoom}
\alias{set_zoom}
\title{Set zoom and container constraints}
\usage{
set_zoom(
  x,
  zoom = 1,
  auto_fit = TRUE,
  max_width = NULL,
  max_height = NULL,
  show_controls = TRUE
)
}
\arguments{
\item{x}{An htmlwidget created by \code{tabviz()}}

\item{zoom}{Zoom level (0.5 to 2.0, default 1.0)}

\item{auto_fit}{When TRUE (default), shrink content to fit container if too large.
Never enlarges content. When FALSE, render at zoom level with scrollbars if needed.}

\item{max_width}{Maximum container width in pixels (NULL for none)}

\item{max_height}{Maximum container height in pixels (NULL for none)}

\item{show_controls}{Show zoom controls on hover}
}
\value{
The modified htmlwidget
}
\description{
Provides a fluent API for controlling zoom level and container size constraints.
}
\examples{
\dontrun{
# Zoom to 80\% with auto-fit
forest_plot(data, point, lower, upper) |>
  set_zoom(zoom = 0.8)

# Constrain to max 800px width
forest_plot(data, point, lower, upper) |>
  set_zoom(max_width = 800)

# Disable auto-fit, hide controls
forest_plot(data, point, lower, upper) |>
  set_zoom(auto_fit = FALSE, show_controls = FALSE)
}

}
